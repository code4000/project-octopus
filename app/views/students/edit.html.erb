<div class="container">
  <h1>Edit <%= @student.name %></h1>
  <%= form_for @student, url: student_path do |form| %>
    <%= render "form", form: form %>
  <% end %>
  <%= link_to 'Back', :back %>
</div>
