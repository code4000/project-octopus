added a comment to

<% if activity.trackable %>
  <%= link_to activity.trackable.resource.name, activity.trackable.resource %>;
  <i>"<%= activity.trackable.body %>"</i>
<% else %>
  which has since been removed
<% end %>
