<div class="container">
  <h1>Edit <%= @contact.name %></h1>
  <%= form_for @contact, url: contact_path do |form| %>
    <%= render "form", form: form %>
  <% end %>
  <%= link_to 'Back', :back %>
</div>
